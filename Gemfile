source 'https://rubygems.org'

ruby '3.3.6'

gem 'parallel'     # 並列処理用
gem 'activesupport' # 汎用ユーティリティ

group :development, :test do
  gem 'rspec'
  gem 'pry'          # デバッグ用
  gem 'pry-byebug'   # ステップ実行デバッグ用
end